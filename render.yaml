services:
  - type: web
    name: propedge-v2
    runtime: node
    region: oregon
    plan: free
    branch: main
    buildCommand: npm install --omit=dev
    startCommand: node dist/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
